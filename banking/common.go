package banking

// DO DO _ READ THESE VALUES FROM SOMEWHERE
var fileIdentifier = "DartC4DFile.exe"
var routingGateway = "656565"

/*
func checkError(message string, err error) {
	if err != nil {
		log.Fatal(message, err)
	}
}
*/
